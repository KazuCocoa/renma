{
  "name": "renma",
  "version": "0.1.0",
  "description": "Minimal-dependency CLI for reviewing AI-agent skills and repo instructions.",
  "type": "module",
  "bin": {
    "renma": "./dist/index.js"
  },
  "files": [
    "dist",
    "scripts"
  ],
  "engines": {
    "node": ">=22.17"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "format": "prettier --write \"**/*.{ts,js,json}\"",
    "format:check": "prettier --check \"**/*.{ts,js,json}\"",
    "lint": "eslint \"{src,test}/**/*.ts\" \"scripts/**/*.mjs\" \"eslint.config.js\"",
    "split-reference": "node scripts/split-reference.mjs",
    "suggest-semantic-split": "node scripts/suggest-semantic-split.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.15.30",
    "eslint": "^10.5.0",
    "eslint-config-prettier": "10.1.8",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.61.0"
  }
}
